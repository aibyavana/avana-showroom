// Site tempo — sultry, slow, intentional. Tune once, applies everywhere.
export const EASE_SULTRY  = [0.22, 1, 0.36, 1] as const;
export const DUR_SLOW     = 1.6;   // primary fade / reveal duration (s)
export const DUR_HOVER    = 0.6;   // hover transition duration (s)
export const DELAY_WITHHOLD = 0.3; // pause before reveal fires (s)
export const DUR_DRAW     = 2.6;   // line-figure stroke-dashoffset draw duration (s)
